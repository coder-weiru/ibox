{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://iplanner-api.ibox.com/event.schema.json",

  "event-array": {
    "type": "array",
    "items": {
      "$ref": "#/event"
    }
  },

  "event": {
    "title": "Event",
    "description": "An event in iplanner api",
    "type": "object",
    "properties": {
      "id": {
        "description": "The unique identifier for the event",
        "type": "string"
      },
      "summary": {
        "description": "The summary of the event",
        "type": "string",
        "minLength": 1
      },
      "description": {
        "description": "The description of the event",
        "type": "string"
      },
      "activity": {
        "description": "The associated activity identifier of the event",
        "type": "string",
        "minLength": 1
      },
      "created": {
        "description": "The timestamp when the event is created",
        "type": "string",
        "minLength": 1,
        "format": "date-time"
      },
      "updated": {
        "description": "The timestamp when the event is updated",
        "type": "string",
        "minLength": 1,
        "format": "date-time"
      },
      "start": {
        "description": "The timestamp when the event starts",
        "type": "string",
        "minLength": 1,
        "format": "date-time"
      },
      "end": {
        "description": "The timestamp when the event ends",
        "type": "string",
        "minLength": 1,
        "format": "date-time"
      },
      "status": {
        "description": "The status of the event",
        "type": "string"
      },
      "creator": {
        "$ref": "#/definitions/user"
      },
      "location": {
        "description": "The location where the event takes place",
        "type": "string"
      },
      "endTimeUnspecified": {
        "description": "Whether the end time has been specified for the event",
        "type": "boolean"
      },
      "recurrence": {
        "description": "The recurrence of the event",
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "required": [
      "summary",
      "activity",
      "created",
      "creator",
      "start"
    ]
  },

  "definitions": {
    "user": {
      "type": "object",
      "properties": {
        "id": {
          "description": "The unique identifier for the user",
          "type": "string",
          "minLength": 1
        },
        "email": {
          "description": "The email for the user",
          "type": "string",
          "format": "email"
        },
        "displayName": {
          "description": "The display name for the user",
          "type": "string",
          "minLength": 1
        },
        "self": {
          "description": "Whether this creator is the user himself",
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "self"
      ]
    }
  }
}